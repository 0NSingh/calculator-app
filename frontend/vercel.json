{
  "buildCommand": "cd .. && npm install && pip install -e ./backend && npm run test:frontend && npm run test:backend && cd frontend && npm run build:frontend",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "installCommand": "npm install && pip install -e ./backend"
}