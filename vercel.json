{
  "builds": [
    {
      "src": "frontend/package.json",
      "use": "@vercel/next"
    }
  ],
  "buildCommand": "([ -d frontend ] && cd frontend && npm install && npm run build) || (npm install && npm run build)",
  "outputDirectory": "frontend/.next"
}